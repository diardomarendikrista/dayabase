# Statistik Pelamar - Bar
SELECT education, COUNT(*) FROM applicants GROUP BY education;

# Analisis Lowongan Kerja - Donut
SELECT j.title, COUNT(a.id) AS total_applicants
FROM jobs j
LEFT JOIN applications a ON j.id = a.job_id
GROUP BY j.title;

# All jobs - Line , Max Salary Bar, Min Salary Bar, 
SELECT * FROM jobs

# Statistik Pendidikan Pelamar (Table)
SELECT education, COUNT(*) FROM applicants GROUP BY education

# All Applicants - Table
SELECT * FROM applicants

# bawah buat nanti

# Evaluasi Tes
SELECT t.test_name, AVG(tr.score) AS avg_score
FROM test_results tr
JOIN tests t ON tr.test_id = t.id
GROUP BY t.test_name;

# Gabungan Multi-Tabel
SELECT 
  a.name,
  j.title,
  t.test_name,
  tr.score,
  tr.passed
FROM applicants a
JOIN applications ap ON a.id = ap.applicant_id
JOIN jobs j ON ap.job_id = j.id
JOIN test_results tr ON ap.id = tr.application_id
JOIN tests t ON tr.test_id = t.id
WHERE j.title = 'Backend Developer';


# Status Aplikasi
SELECT status, COUNT(*) FROM applications GROUP BY status;



Next?
- Auth dulu
- Tambah model chart dulu
- Filter Dashboard (major, karena ubah fullstack + cara tulis query)